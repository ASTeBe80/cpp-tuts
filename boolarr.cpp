#include <bits/stdc++.h>
using namespace std;

int ftrue(vector<int> &arr, int n){
    int target
    int low=0;
    int high=n-1;
    int mid;
    while(high >= low){
        mid = (high + low)/2;

        if()
    }
}

int main() {
    int t = 
    vector<int> arr = {0,0,0,1,1};
    ftrue(arr,t);
    return 0;
}